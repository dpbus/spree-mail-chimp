<div id="mailchimp_subscribe_wrap">
  <%= form_for :user, :url => :subscriptions do |f| %>
    <%= text_field_tag :email, nil, :class => 'email', :id => 'subscribe_email', :placeholder => t('enter_your_email') %>
    <%= submit_tag t('subscribe'), :id => 'op_subscribe' %>
  <% end %>
</div>